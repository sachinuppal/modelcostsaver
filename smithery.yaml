# Smithery configuration: https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    {}
  commandFunction:
    |-
    (config) => ({
      "command": "npx",
      "args": ["-y", "@workswarm/modelcostsaver"]
    })
